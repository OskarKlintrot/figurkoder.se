{
  "name": "figurkoder.se",
  "version": "1.0.0",
  "description": "En webbaserad träningsapp för figurkoder.",
  "main": "src/index.html",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "serve": "npx http-server src -p 3000 -c-1 --cors",
    "serve:test": "npx http-server src -p 3001 -c-1 --cors"
  },
  "keywords": [
    "figurkoder",
    "memory training"
  ],
  "author": "Oskar Klintrot",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
